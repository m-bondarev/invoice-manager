ostorage:
  region: ${OCI_CLI_REGION}
  nameSpace: frkogu3mhwjt
  bucketName: maryTestBucket
  tenancyId: ${OCI_CLI_TENANCY}
  userId: ${OCI_CLI_USER}
  fingerprint: ${OCI_CLI_FINGERPRINT}
  privateKey: ${OCI_CLI_KEY_CONTENT}